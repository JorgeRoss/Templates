import { ParallaxInitDirective } from './parallax-init.directive';

describe('ParallaxInitDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxInitDirective();
    expect(directive).toBeTruthy();
  });
});
